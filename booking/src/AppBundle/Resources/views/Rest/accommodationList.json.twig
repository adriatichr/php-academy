{
    "accommodations": [
        {% for accommodation in accommodations %}
            {
                "id": {{ accommodation.id }},
                "uri": "{{ url('AppBundle_Rest_accommodation', {'accommodationId': accommodation.id}) }}",
                "actions": ["GET", "DELETE"]
            }{% if not loop.last %},{% endif %}
        {% endfor %}
    ]
}
